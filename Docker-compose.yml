services:
  laravel-docker:
    container_name: laravel-docker
    build: ./rando-laravel-NOUVELLE-BRANCHE-MAIN-api
    ports:
      - 8000:80

  react-docker:
    container_name: react-docker
    build: ./Projet-rando-front
    ports:
      - 80:80

  mysql_db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: OPL2025
      MYSQL_DATABASE: rando-ouest
    ports:
      - 3306:3306