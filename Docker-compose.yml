services:
  laravel-docker:
    container_name: laravel-docker
    build: ./rando-laravel-NOUVELLLE-BRANCHE-MAIN-api
    ports:
      - 8000:80

  vue-docker:
    container_name: vue-docker
    build: ./rando-rando-front
    ports:
      - 80:80

  mysql_db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: OPL2025
      MYSQL_DATABASE: rando-ouest
    ports:
      - 3306:3306